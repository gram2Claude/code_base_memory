# Init: code_base_memory

**Дата:** 2026-06-12
**Источник:** аргумент `/workflow_global_plan` от управленца (дословно):

> я хочу создать универсиальный скил памяти кодовой базы в каждом проекте, где это необходимо
> - для начала /security-review полностью проверь на безопасность эти файлы ontoindex
> - этот скил должен стать частью workflow_global_plan. При вызове этой команды должно появляться
>   сообщение с вопросом - Активировать кодовую память или нет
> - внутри проекта при работе с кодом claude должен использовать все доступные возможности
>   ontoindex, чтобы минимизировать свои ошибки при работе с рабочими файлами, функциями,
>   классами и пр.
> - все, что мы забрали из ontoindex теперь должно жить локально у нас и не зависеть от
>   исходного репозитория

## Статус подготовительных шагов

- [x] Security review ontoindex выполнен 2026-06-12 → `work_directory/05_security/00_ontoindex_security_review.md`.
      Вердикт: пригоден для вендоринга; обязательные меры — удалить @scarf/scarf, не настраивать
      LLM-ключи, MCP только stdio read-only, заменить `npx ontoindex@latest` в хуках на локальный путь.
- [x] Репозиторий проекта: https://github.com/gram2Claude/code_base_memory (public, main запушен).
- [x] Timechecker: slug `code_base_memory`, prefix `CBM`, branch `oleg`.
- [ ] start_date — не задана (вопрос управленцу).
- [ ] Спека — черновик `00_code_base_memory_spec.md`, открытые вопросы внутри.

## Конфликт, требующий решения

Требование «не зависеть от исходного репозитория» ⇔ текущее подключение `ontoindex/` как
git-SUBMODULE (ссылается на github.com/ontograph/ontoindex). Submodule НЕ удовлетворяет
требованию: исчезнет апстрим — сломается clone. Решение — в спеке (вопрос Q2).
